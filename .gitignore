# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
ENV/

# Data and Logs
logs/
data/cache/
data/trades_backup.json
temp/
tmp/

# Models (Ignore large checkpoinst, keep active ones if small enough or use LFS)
# We keep active models in models/, archive is ignored? No, user wants structure.
# But we might want to ignore very large files if they exceed GitHub limits (100MB).
# stop_hunt_rf.joblib is ~6MB. infinite_rf is ~400KB.
# Gatekeeper is small.

# OS
.DS_Store
.env
.ipynb_checkpoints
.idea/
.vscode/

# Credentials
config/credentials.json
config/api_keys.json
